using SalesSystem.System.MVC.Entities;
using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.ComponentModel.DataAnnotations;
using System.Linq;

namespace SalesSystem.System.MVC.Models.ViewModels
{
    public class DepartmentViewModel
    {
        //[Key]
        //public int Id { get; set; }

        //[DisplayName("Registration Date")]
        //[DataType(DataType.Date)]
        //[DisplayFormat(DataFormatString = "{0:dd/MM/yyyy - hh:mm:ss}")]
        //public DateTime RegistrationDate { get; set; }

        //[Required(ErrorMessage = "Required field")]
        //[DisplayName("Department Name")]
        //[MaxLength(80, ErrorMessage = "have to have at most {1} Caract")]
        //[MinLength(2, ErrorMessage = "have to have at least {1}")]
        //public string DepartmentName { get; set; }

        //public ICollection<Seller> Sellers { get; set; } = new List<Seller>();

        //public void AddSeller(Seller seller)
        //{
        //    Sellers.Add(seller);
        //}

        //public decimal TotalSales(DateTime initial, DateTime final)
        //{
        //    return Sellers.Sum(seller => seller.TotalSales(initial, final));
        //}
    }
}
